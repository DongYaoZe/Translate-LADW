# 历史草稿归档

本目录保存过去曾位于 `part/` 下、但当前 `main.tex` 不再引用的历史草稿与旧题解版本。

- `exerGPT.tex`：旧的 GPT 题解草稿。
- `exerGem2.5.tex`：旧的 Gemini 题解草稿。
- `manipulate.tex`：历史操作草稿（当前为空文件）。
- `appendix.tex`：旧附录草稿；其中引用的 `image1.eps` 已不在项目中，因此不能作为当前构建组件使用。

这些文件仅用于保留历史，不应被视为与当前 2026-04-30 原书同步的正式正文。正式构建只使用 `main.tex` 中实际 `\input` 的文件。
